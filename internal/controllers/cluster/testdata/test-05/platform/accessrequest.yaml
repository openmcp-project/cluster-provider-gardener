apiVersion: clusters.openmcp.cloud/v1alpha1
kind: AccessRequest
metadata:
  name: my-access
  namespace: foo
spec:
  clusterRef:
    name: advanced
    namespace: clusters

  token:
    permissions:
    - rules:
      - apiGroups:
        - "*"
        resources:
        - "*"
        verbs:
        - "*"
    - name: bar
      namespace: foo
      rules:
      - apiGroups:
        - "*"
        resources:
        - "*"
        verbs:
        - "*"
    roleRefs:
    - kind: Role
      name: secret-viewer
      namespace: default
    - kind: ClusterRole
      name: cluster-admin
