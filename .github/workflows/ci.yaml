name: CI

on:
  push:
    tags:
      - v*
    branches:
      - master
      - main
  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: openmcp-project/build/.github/workflows/ci.lib.yaml@main
